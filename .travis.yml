language: node_js
node_js:
  - "stable"
cache: npm
install:
- npm install
script:
- npm run development
servies:
- mongodb+srv://serler:serlersdm@cluster0-bwx14.mongodb.net/test?retryWrites=true&w=majority

deploy:
  provider: pages
  skip-cleanup: true
  github-token: $GITHUB_TOKEN
  local_dir: build
  on:
    branch: develop
